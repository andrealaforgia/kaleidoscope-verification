# Harness scratch space (host bind for the otelcol-sink capture file
# and any other compose-side artefacts). Per-run evidence is moved
# into expectations/<ID>/evidence/ by run-expectation.sh; what remains
# here is transient.
harness/.captured/

# macOS
.DS_Store

# Editor scratch
*.swp
*.swo
