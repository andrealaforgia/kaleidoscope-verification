LINKEDVIEW satisfied — the view's single call (with_logs) on the bundled demo trace returns the WHOLE coherent failed checkout in ONE response: a checkout-shaped Error span ('POST /api/v1/checkout', 'checkout failed: card declined') = WHERE, AND its single clean cause log = WHY, both scoped to the trace, no orphaned/duplicate copies. The data linkage the view renders is ready; the on-screen view itself is proven only by the Customer's cold browser run on the clean managed instance.
