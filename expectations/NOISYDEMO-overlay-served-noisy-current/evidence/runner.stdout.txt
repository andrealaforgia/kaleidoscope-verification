NOISYDEMO satisfied — the overlay-served demo (no seed, now-relative, always-current) is NOISY and coherent: 12 logs (mostly non-error, exactly 1 declined) and 6 traces across 3 customers with exactly 1 failed checkout (customer.id=alice). Search discriminates: body_regex=(?i)DECLINED and min_severity=error each -> the one log; attr customer.id=alice -> that customer, + error=true -> the one failed checkout, pivoting to WHERE + WHY (single clean cause, no orphans). A newcomer's cold run discriminates a real failure out of noise.
