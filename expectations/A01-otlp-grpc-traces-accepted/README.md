# A01 — otlp-grpc-traces-accepted

## Surface

Aperture (OTLP ingest gateway). Operator/integrator-facing.

## Behaviour

Given aperture is running with both listeners bound (gRPC `:4317`, HTTP
`:4318`)
And a sink is configured (any of `stub` or `forwarding`; A01 is
agnostic to the choice — round-trip-to-downstream is E01's job)
When an OTLP/gRPC client sends an `ExportTraceServiceRequest`
containing N spans to `:4317`
Then aperture acks the request (gRPC OK)
And aperture's stderr emits one
`event=sink_accepted signal=traces span_count=N` line
naming the resource's `service.name`.

## Source

- Inter-session feed (other claude session, 2026-05-06): item **A1**.
- External contract anchor:
  [`docs/feature/aperture/slices/slice-03-traces.md`](https://github.com/andrealaforgia/kaleidoscope/blob/3d3c99f061a3c76d48ac9d2a824612d8bdc37b68/docs/feature/aperture/slices/slice-03-traces.md)
  line 42 ("Real `ExportTraceServiceRequest` on gRPC -> gRPC OK +
  sink_accepted stderr line with `signal=traces`.").

## Verification

- Status: `satisfied`
- Last verified: 2026-05-27 UTC at HEAD (`a6175f1`). Cycle 22
  cold retry GREEN at first attempt
  ("aperture acked the OTLP/gRPC request and emitted
  sink_accepted with matching span_count and service.name").
  Cycle-21 double-fail confirmed flake, same disposition as
  closed issues 006/007.
- Earlier satisfaction: 2026-05-06T23:11:33Z (re-verification after issue
  001 fix; aperture now honours `--config` and the harness's
  `kind = "forwarding"` actually applies). First verification at SHA
  `3d3c99f061a3c76d48ac9d2a824612d8bdc37b68` on 2026-05-06T19:37:52Z
  also passed via the StubSink path; both runs are kept on the same
  pinning record updated in place.
- Kaleidoscope SHA: `6b09c0d4eb38fc2e83a4fc8cf3f9bad6d9813b15`
- Kaleidoscope dirty: `no`
- Method: dockerised harness; aperture built from the HEAD snapshot,
  invoked with `--config /etc/aperture/aperture.toml`
  (`kind = "forwarding"`, downstream
  `http://otelcol-sink:4318`). OTLP/gRPC traffic is generated by
  the upstream `telemetrygen` image
  (`ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:v0.114.0`)
  which sends one trace (parent + child = 2 spans) tagged
  `service.name="expectation-A01-pilot"` to `aperture:4317` over
  the compose network. Telemetrygen exits 0 (proves the gRPC OK
  ack); the runner then greps aperture's container stderr for the
  `event=sink_accepted` line that names the matching service and
  span_count.

## Evidence

- [`evidence/verification.yaml`](evidence/verification.yaml) —
  pinning record (SHA, dirty flag, host, timestamp).
- [`evidence/runner.stdout.txt`](evidence/runner.stdout.txt) — runner
  log: aperture ready, telemetrygen exit 0, sink_accepted matched.
- [`evidence/telemetrygen.stderr.txt`](evidence/telemetrygen.stderr.txt) —
  the gRPC client's own log; the `Channel Connectivity change to
  READY` then `SHUTDOWN` sequence proves the export round-tripped
  to OK.
- [`evidence/aperture.live.stderr.txt`](evidence/aperture.live.stderr.txt) —
  aperture's container stderr captured live during the run; the
  matched line is:
  `{"timestamp":"2026-05-06T23:11:33.682101Z","level":"INFO","event":"sink_accepted","sink":"forwarding","downstream":"http://otelcol-sink:4318","signal":"traces","span_count":2,"downstream_latency_ms":2,"resource.service.name":"expectation-A01-pilot"}`
- [`evidence/otlp-received.jsonl`](evidence/otlp-received.jsonl) —
  the otelcol-sink's file-exporter capture (984 bytes). Now
  non-empty: `resourceSpans` carrying our `service.name` and the
  telemetrygen-generated `traceId` / `spanId` round-tripped end to
  end through aperture's ForwardingSink. Independent third-party
  evidence beyond aperture's own stderr.

## Issues

- [`issues/001-aperture-binary-ignores-config-flag.md`](../../issues/001-aperture-binary-ignores-config-flag.md) —
  closed at 6b09c0d. Verification at this SHA shows
  `sink="forwarding"` and `downstream="http://otelcol-sink:4318"`,
  matching the contract.

## Notes

None.
