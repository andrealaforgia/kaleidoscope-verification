FAIL: payment.amount comes back as a STRING, not a number: ["'100'", "'1500'", "'250'", "'9'", "'90'"]. The platform coerces numeric attribute values to strings; a numeric >= comparison cannot be genuine until this is fixed.
