LOGSEARCH satisfied — log search discriminates the failure out of noise and pivots: 12 logs, exactly 1 declined; body_contains=declined -> that one (with trace_id 7542b887614a210d59c01249ed435ce9), case-sensitive ('Declined'->0); body_regex=(?i)declined AND (?i)DECLINED each -> that one (the CASE-INSENSITIVE search the view issues, verified on the REAL backend, not mocked); min_severity=error -> the one error out of INFO noise; the matched log's trace resolves to WHERE + WHY. The on-screen Logs view + pivot rest on this; her cold run is the gate.
