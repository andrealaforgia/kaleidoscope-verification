LOGSEARCH satisfied — searching logs by text and severity discriminates the failure out of noise, and the match pivots: 12 logs in window, exactly 1 declined; body_contains=declined -> that one log only (carrying its trace_id, 0c49b573f01e41deb23354e24296dbe1), case-sensitive ('Declined' -> 0); min_severity=error -> the one error out of the info noise; and the matched log's trace resolves to WHERE (an Error span) + WHY (the cause log). The on-screen Logs view + pivot rest on this; her cold run is the gate.
