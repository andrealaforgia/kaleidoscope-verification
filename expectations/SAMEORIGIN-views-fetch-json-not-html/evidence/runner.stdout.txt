SAMEORIGIN satisfied — every on-screen view's data fetch on its own origin (:9090) returns the signal's JSON: metrics=application/json, logs=application/json, logs_rx=application/json, traces=application/json, with_logs=application/json. The same-origin glue each view actually uses is exercised against the real backend, so the HTML/empty-where-JSON-expected class is caught here, not only by the Customer's cold run.
