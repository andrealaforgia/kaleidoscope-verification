FAIL: on its own origin (:9090), the data fetch for these view(s) does NOT return the signal's JSON, so the view throws 'Unexpected token <' / cannot render: logs -> (none) [empty/non-JSON (route not served same-origin)]; logs_rx -> (none) [empty/non-JSON (route not served same-origin)]. The route is not merged same-origin (it falls through to the SPA catch-all on the deployed stack, or is simply not served on :9090 here). Traces/metrics ARE merged; the logs route is the gap.
