diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 49a2f81..e539d48 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -177,15 +177,15 @@ jobs:
         #
         # `--workspace` covers the harness, aperture, and spark.
         # The harness graduated at its v0 DELIVER close (2026-05-04).
-        # Aperture graduated at its v0 DELIVER close (2026-05-05).
-        # Spark graduated at its v0 DELIVER close (2026-05-06) after
-        # Crafty in review mode approved the wave on iteration one
-        # with no blocking issues. Sieve graduated at its v0 DELIVER
-        # close (2026-05-07) on the same review verdict — 36 active
-        # tests across eight binaries, 100% mutation kill rate on the
-        # diff at every slice's close. All four crates ship green
-        # tests on every commit and Gate 1 enforces that.
-        run: cargo test --workspace --all-targets --locked
+        # Aperture, Spark, Sieve all graduated through v0 DELIVER
+        # close. Codex is currently mid-DISTILL/DELIVER (its acceptance
+        # tests panic on `unimplemented!()` by design); excluded here
+        # until v0 DELIVER closes, mirroring the precedent set for
+        # Sieve and Spark in their RED phases. Compile-only coverage
+        # of Codex during this phase is enforced by the local
+        # pre-commit hook's `cargo build -p codex --all-targets
+        # --locked` step.
+        run: cargo test --workspace --exclude codex --all-targets --locked
 
       # KPI 4 verdict-counts artefact (Crafty's Q5).
       #
diff --git a/Cargo.toml b/Cargo.toml
index a9da22c..5278e93 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -5,6 +5,7 @@ members = [
     "crates/aperture",
     "crates/spark",
     "crates/sieve",
+    "crates/codex",
 ]
 
 # Shared workspace metadata. Per ADR-0003 and ADR-0005 the
diff --git a/scripts/hooks/pre-commit b/scripts/hooks/pre-commit
index a40f97a..fe8b380 100755
--- a/scripts/hooks/pre-commit
+++ b/scripts/hooks/pre-commit
@@ -86,17 +86,23 @@ else
 fi
 
 # Step 4: cargo test --all-targets --locked (Gate 1) --------------------
-# Workspace-wide. The harness graduated at its v0 DELIVER close
-# (2026-05-04). Aperture graduated at its v0 DELIVER close
-# (2026-05-05). Spark graduated at its v0 DELIVER close (2026-05-06)
-# after Crafty in review mode approved the wave on iteration one
-# with no blocking issues. Sieve graduated at its v0 DELIVER close
-# (2026-05-07) on the same review verdict. All four crates ship
-# green tests on every commit and the hook enforces that.
-echo "→ cargo test --workspace --all-targets --locked  (Gate 1)"
-if ! cargo test --workspace --all-targets --locked; then
+# Workspace-wide except Codex. Harness, Aperture, Spark, Sieve all
+# graduated through v0 DELIVER close at the dates noted in their
+# respective slice-08-completion / DELIVER review docs. Codex is
+# currently mid-DISTILL/DELIVER: its acceptance tests panic on
+# `unimplemented!()` by design (Strategy C "real local" RED-first
+# posture). Excluded here until Codex graduates at the close of its
+# v0 DELIVER cycle, mirroring the precedent set when Aperture, Spark,
+# and Sieve were each in the same phase.
+echo "→ cargo test --workspace --exclude codex --all-targets --locked  (Gate 1)"
+if ! cargo test --workspace --exclude codex --all-targets --locked; then
   red "[fail] cargo test"
   exit 1
 fi
+echo "→ cargo build -p codex --all-targets --locked  (Gate 1, Codex compile-only)"
+if ! cargo build -p codex --all-targets --locked; then
+  red "[fail] cargo build -p codex"
+  exit 1
+fi
 
 green "[pass] all pre-commit gates green"
